// This is a basic Flutter widget test.
//
// To perform an interaction with a widget in your test, use the WidgetTester
// utility that Flutter provides. For example, you can send tap and scroll
// gestures. You can also use WidgetTester to find child widgets in the widget
// tree, read text, and verify that the values of widget properties are correct.

import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Sign In Navigation', () {
    test('Move To Login and back', () {
      // todo test moving to the login screen, then going 'back'
    });
    test('Move To Register and back', () {
      // todo test moving to the Register screen, then going 'back'
    });
    test('Move to Login then Register then back back', () {
      // todo test moving tp the login screen, register screen
      // then going 'back' twice
    });
    test('Move to Register then Login then back back', () {
      // todo test moving tp the register screen, login screen
      // then going 'back' twice
    });
  });
}
